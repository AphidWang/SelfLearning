{
  "name": "self-learning-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd apps/client && yarn dev\" \"cd apps/server && yarn dev\"",
    "build": "yarn workspaces run build",
    "start": "cd apps/server && yarn start"
  }
}
